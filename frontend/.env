# IP da sua máquina na rede Wi-Fi (mesma do celular). Detectado automaticamente:
# 192.168.0.111 — confirme com `ip addr` se mudou. O backend deve subir em 0.0.0.0:8000.
EXPO_PUBLIC_API_URL=http://192.168.0.111:8000
