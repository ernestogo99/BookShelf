# URL da API. Em CELULAR FÍSICO use o IP da sua máquina na rede Wi-Fi.
# Descubra o IP com `ip addr` (Linux) / `ifconfig` (Mac) — procure algo como 192.168.x.y
# O backend deve subir em 0.0.0.0:  uvicorn app.main:app --host 0.0.0.0 --port 8000
EXPO_PUBLIC_API_URL=http://192.168.0.10:8000
